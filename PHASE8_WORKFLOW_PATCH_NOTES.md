Add `scripts/seo_control_self_test.py`; run audit-only GitHub workflow before AI or approved implementation tests.

## Gap fixed

Added `scripts/run_daily_pipeline.py` starter audit pipeline so the workflow no longer hard-fails due to a missing core audit script.
